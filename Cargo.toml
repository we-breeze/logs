[package]
name = "log"
version = "0.1.0"
authors = ["icy"]
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# 日志宏已降级为 no-op，无外部依赖。

[features]
enable-log = []
